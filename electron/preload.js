// Preload — corre antes que los scripts del documento, con acceso al
// localStorage del origin (http://127.0.0.1:PORT). Sirve de bridge entre
// el main process y el renderer.
//
// Por ahora está vacío. En Fase 3 se inyecta acá el JWT del admin para
// saltarse la pantalla de login.
